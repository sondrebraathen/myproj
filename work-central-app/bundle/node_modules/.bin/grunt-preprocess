#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-preprocess/bin/grunt-preprocess" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-preprocess/bin/grunt-preprocess" "$@"
  ret=$?
fi
exit $ret
